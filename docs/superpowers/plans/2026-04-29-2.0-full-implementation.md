# Learn++ 2.0 全功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 2.0 UI 框架完成所有遗留功能的真实业务实现，涵盖数据统计、课件预览、甘蔗 Tutor 全局 Agent、邮箱接入、全局动画与 UI 收尾。

**Architecture:** P1 数据引擎打底（stats + search-index + 学期切换）→ P2/P3/P4 三路并行（课件工作流、Tutor Agent、邮箱接入）→ P5 UI 收尾（全局动画、窗口修复、CSS 清理）。每层新增 IPC 同步更新 preload/index.ts + api.d.ts + env.d.ts。

**Tech Stack:** Electron 31, React 18, TypeScript 5, Zustand, Ant Design 5, thu-learn-lib, Node http/https

---

## P1: 数据与统计引擎

### Task 1: 添加 stats.json 路径和 stats 类型定义

**Files:**
- Modify: `src/main/utils/paths.ts`
- Modify: `src/main/types.ts`

- [ ] **Step 1: 在 paths.ts 添加 statsFile 路径**

在 `src/main/utils/paths.ts` 中，`settingsFile` 行后添加：

```ts
export const statsFile = path.join(userDataPath, 'stats.json')
```

- [ ] **Step 2: 在 types.ts 添加统计类型**

在 `src/main/types.ts` 末尾追加：

```ts
export interface StatsSnapshot {
  todayMinutes: number
  streakDays: number
  completedCourses: number
  totalCourses: number
  weeklyMinutes: number
  todayFocus: TodayFocusItem[]
  courseProgress: CourseProgressItem[]
  recentUpdates: RecentUpdateItem[]
}

export interface TodayFocusItem {
  priority: 'P0' | 'P1' | 'P2'
  courseName: string
  courseId: string
  title: string
  tag: string
  deadline?: string
  targetTab: 'homework' | 'notifications' | 'discussion' | 'files'
  targetId?: string
}

export interface CourseProgressItem {
  courseId: string
  courseName: string
  done: number
  total: number
  percent: number
}

export interface RecentUpdateItem {
  courseId: string
  courseName: string
  text: string
  time: string
  kind: 'notice' | 'file' | 'homework' | 'discussion'
}

export interface StatsDailyRecord {
  dailyMinutes: Record<string, number>
  lastActiveDate: string
}
```

- [ ] **Step 3: 运行 typecheck 确认无类型错误**

```bash
npm run typecheck
```

- [ ] **Step 4: Commit**

```bash
git add src/main/utils/paths.ts src/main/types.ts
git commit -m "feat: add stats types and stats.json path"
```

### Task 2: 实现 stats.ts 学习统计服务

**Files:**
- Create: `src/main/services/stats.ts`

- [ ] **Step 1: 创建 stats.ts**

```ts
import fs from 'fs'
import { statsFile } from '../utils/paths'
import type { StatsSnapshot, StatsDailyRecord, CourseProgressItem, RecentUpdateItem, TodayFocusItem } from '../types'

let dailyMinutes: Record<string, number> = {}
let lastActiveDate = ''
let activeSince: number | null = null

function todayKey(): string {
  return new Date().toISOString().slice(0, 10)
}

function loadDailyRecords(): void {
  try {
    const raw = fs.readFileSync(statsFile, 'utf-8')
    const data: StatsDailyRecord = JSON.parse(raw)
    dailyMinutes = data.dailyMinutes || {}
    lastActiveDate = data.lastActiveDate || ''
  } catch {
    dailyMinutes = {}
    lastActiveDate = ''
  }
}

function saveDailyRecords(): void {
  const data: StatsDailyRecord = { dailyMinutes, lastActiveDate }
  fs.writeFileSync(statsFile, JSON.stringify(data, null, 2))
}

export function startTracking(): void {
  loadDailyRecords()
  activeSince = Date.now()
}

export function stopTracking(): void {
  if (activeSince == null) return
  const elapsed = Math.round((Date.now() - activeSince) / 60000)
  activeSince = null
  if (elapsed < 1) return
  const key = todayKey()
  dailyMinutes[key] = (dailyMinutes[key] || 0) + elapsed
  lastActiveDate = key
  saveDailyRecords()
}

export function pauseTracking(): void {
  if (activeSince == null) return
  const elapsed = Math.round((Date.now() - activeSince) / 60000)
  if (elapsed < 1) return
  const key = todayKey()
  dailyMinutes[key] = (dailyMinutes[key] || 0) + elapsed
  lastActiveDate = key
  activeSince = null
  saveDailyRecords()
}

export function resumeTracking(): void {
  loadDailyRecords()
  activeSince = Date.now()
}

function computeStreakDays(): number {
  let streak = 0
  const d = new Date()
  // Walk backwards from today; stop when a day has < 30 min
  for (let i = 0; i < 365; i++) {
    const key = d.toISOString().slice(0, 10)
    if ((dailyMinutes[key] || 0) >= 30) {
      streak++
      d.setDate(d.getDate() - 1)
    } else if (key === todayKey()) {
      // Today not yet 30min, keep checking yesterday
      d.setDate(d.getDate() - 1)
    } else {
      break
    }
  }
  return streak
}

function computeWeeklyMinutes(): number {
  let total = 0
  const d = new Date()
  for (let i = 0; i < 7; i++) {
    const key = d.toISOString().slice(0, 10)
    total += dailyMinutes[key] || 0
    d.setDate(d.getDate() - 1)
  }
  return total
}

export function computeDashboard(
  courses: { id: string; name: string; teacher: string }[],
  homeworksByCourse: Map<string, any[]>,
  noticesByCourse: Map<string, any[]>,
  discussionsByCourse: Map<string, any[]>,
): StatsSnapshot {
  loadDailyRecords()

  // Course progress (homework only)
  const courseProgress: CourseProgressItem[] = courses.map((c) => {
    const hws = homeworksByCourse.get(c.id) || []
    const done = hws.filter((h: any) =>
      h.status === '已提交' || h.status === '已批阅' || h.submitted === true
    ).length
    const total = hws.length || 1
    return { courseId: c.id, courseName: c.name, done, total, percent: Math.round((done / total) * 100) }
  })

  const completedCourses = courseProgress.filter((cp) => cp.percent === 100).length

  // Today focus - three tiers
  const now = new Date()
  const todayEnd = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 23, 59, 59)
  const threeDaysLater = new Date(todayEnd.getTime() + 3 * 86400000)

  const p0Items: TodayFocusItem[] = []
  const p1Items: TodayFocusItem[] = []
  const p2Items: TodayFocusItem[] = []

  for (const c of courses) {
    const hws = homeworksByCourse.get(c.id) || []
    for (const hw of hws) {
      if (hw.status === '已提交' || hw.status === '已批阅') continue
      const dl = new Date(hw.deadline)
      if (isNaN(dl.getTime())) continue
      const item: TodayFocusItem = {
        priority: 'P0',
        courseName: c.name,
        courseId: c.id,
        title: hw.title,
        tag: '',
        deadline: hw.deadline,
        targetTab: 'homework',
        targetId: hw.studentHomeworkId || hw.id,
      }
      if (dl <= todayEnd) {
        item.priority = 'P0'
        item.tag = '今天截止'
        p0Items.push(item)
      } else if (dl <= threeDaysLater) {
        item.priority = 'P1'
        item.tag = '即将截止'
        p1Items.push(item)
      }
    }
  }

  // P2: recent notices and discussions (last 3 days)
  const threeDaysAgo = new Date(now.getTime() - 3 * 86400000)
  for (const c of courses) {
    const notices = noticesByCourse.get(c.id) || []
    for (const n of notices) {
      const pt = new Date(n.publishTime)
      if (!isNaN(pt.getTime()) && pt >= threeDaysAgo) {
        p2Items.push({
          priority: 'P2', courseName: c.name, courseId: c.id,
          title: n.title, tag: '新公告',
          targetTab: 'notifications', targetId: n.id,
        })
      }
    }
    const discs = discussionsByCourse.get(c.id) || []
    for (const d of discs) {
      const pt = new Date(d.publishTime)
      if (!isNaN(pt.getTime()) && pt >= threeDaysAgo) {
        p2Items.push({
          priority: 'P2', courseName: c.name, courseId: c.id,
          title: d.title, tag: '新讨论',
          targetTab: 'discussion', targetId: d.id,
        })
      }
    }
  }

  // Recent updates
  const updates: RecentUpdateItem[] = []
  for (const c of courses) {
    const notices = noticesByCourse.get(c.id) || []
    const files = [] // populated by files IPC caller
    const hws = homeworksByCourse.get(c.id) || []
    if (notices.length) {
      const latest = notices.reduce((a: any, b: any) =>
        new Date(a.publishTime) > new Date(b.publishTime) ? a : b
      )
      updates.push({
        courseId: c.id, courseName: c.name,
        text: `新增公告：${latest.title}`, time: latest.publishTime, kind: 'notice',
      })
    }
    if (hws.length) {
      const latest = hws.reduce((a: any, b: any) =>
        new Date(a.deadline || 0) > new Date(b.deadline || 0) ? a : b
      )
      updates.push({
        courseId: c.id, courseName: c.name,
        text: `作业：${latest.title}`, time: latest.deadline || '', kind: 'homework',
      })
    }
  }

  return {
    todayMinutes: dailyMinutes[todayKey()] || 0,
    streakDays: computeStreakDays(),
    completedCourses,
    totalCourses: courses.length,
    weeklyMinutes: computeWeeklyMinutes(),
    todayFocus: [...p0Items, ...p1Items, ...p2Items],
    courseProgress,
    recentUpdates: updates.sort((a, b) =>
      new Date(b.time).getTime() - new Date(a.time).getTime()
    ).slice(0, 10),
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main/services/stats.ts
git commit -m "feat: implement stats tracking service with dashboard computation"
```

### Task 3: 实现 search-index.ts 全局搜索索引

**Files:**
- Create: `src/main/services/search-index.ts`

- [ ] **Step 1: 创建 search-index.ts**

```ts
interface SearchEntry {
  type: 'course' | 'homework' | 'file' | 'notice' | 'discussion' | 'email'
  title: string
  subtitle: string
  courseId?: string
  courseName?: string
  targetTab?: string
  targetId?: string
  keywords: string[]
}

const index: Map<string, SearchEntry[]> = new Map()

function tokenize(text: string): string[] {
  const cleaned = text.toLowerCase().replace(/[^一-龥a-z0-9]/g, ' ')
  const tokens: string[] = []
  // Character n-grams for Chinese
  const chinese = cleaned.replace(/\s/g, '')
  for (let i = 0; i < chinese.length; i++) {
    tokens.push(chinese.slice(i, i + 1))
    if (i + 1 < chinese.length) tokens.push(chinese.slice(i, i + 2))
  }
  // Word tokens for English
  const words = cleaned.split(/\s+/).filter((w) => w.length > 1)
  tokens.push(...words)
  return [...new Set(tokens)]
}

function buildInvertedIndex(entries: SearchEntry[]): void {
  for (const entry of entries) {
    const tokens = tokenize(entry.title + ' ' + entry.subtitle)
    for (const token of tokens) {
      const existing = index.get(token) || []
      existing.push(entry)
      index.set(token, existing)
    }
  }
}

export function indexCourses(courses: { id: string; name: string; teacher: string }[]): void {
  index.clear()
  const entries: SearchEntry[] = courses.map((c) => ({
    type: 'course', title: c.name, subtitle: c.teacher,
    courseId: c.id, courseName: c.name, targetTab: 'notifications',
    keywords: tokenize(c.name + ' ' + c.teacher),
  }))
  buildInvertedIndex(entries)
}

export function indexItems(
  type: SearchEntry['type'],
  items: { id: string; title: string; courseName?: string; courseId?: string }[],
  targetTab: string,
): void {
  const entries: SearchEntry[] = items.map((item) => ({
    type,
    title: item.title || '',
    subtitle: item.courseName || '',
    courseId: item.courseId,
    courseName: item.courseName,
    targetTab,
    targetId: item.id,
    keywords: tokenize((item.title || '') + ' ' + (item.courseName || '')),
  }))
  buildInvertedIndex(entries)
}

export function indexEmails(emails: { id: string; subject: string; from: string }[]): void {
  const entries: SearchEntry[] = emails.map((e) => ({
    type: 'email',
    title: e.subject,
    subtitle: e.from,
    targetTab: 'mailbox',
    targetId: e.id,
    keywords: tokenize(e.subject + ' ' + e.from),
  }))
  buildInvertedIndex(entries)
}

export function query(q: string, typeFilter?: string): SearchEntry[] {
  const tokens = tokenize(q)
  const results = new Map<string, SearchEntry>()
  for (const token of tokens) {
    const matches = index.get(token) || []
    for (const match of matches) {
      if (typeFilter && match.type !== typeFilter) continue
      results.set(match.type + ':' + (match.targetId || match.title), match)
    }
  }
  // Score by keyword hit count and return top results
  return Array.from(results.values()).slice(0, 20)
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main/services/search-index.ts
git commit -m "feat: implement n-gram search index for global search"
```

### Task 4: 添加 stats 和 search IPC

**Files:**
- Create: `src/main/ipc/stats.ts`

- [ ] **Step 1: 创建 IPC 注册文件**

```ts
import { ipcMain } from 'electron'
import { computeDashboard, startTracking, stopTracking, pauseTracking, resumeTracking } from '../services/stats'
import { query, indexItems } from '../services/search-index'

export function registerStatsIpc(): void {
  ipcMain.handle('stats:getDashboard', async () => {
    // The caller (renderer) provides the data sets via separate calls,
    // or we gather from the learn service. For initial implementation,
    // we accept course list + homework lists as params.
    return { ok: true }
  })

  ipcMain.handle('stats:computeDashboard', async (_e, payload: {
    courses: { id: string; name: string; teacher: string }[]
    homeworksByCourse: Record<string, any[]>
    noticesByCourse: Record<string, any[]>
    discussionsByCourse: Record<string, any[]>
  }) => {
    const homeworksMap = new Map(Object.entries(payload.homeworksByCourse))
    const noticesMap = new Map(Object.entries(payload.noticesByCourse))
    const discussionsMap = new Map(Object.entries(payload.discussionsByCourse))
    return computeDashboard(payload.courses, homeworksMap, noticesMap, discussionsMap)
  })

  ipcMain.handle('search:query', async (_e, q: string, typeFilter?: string) => {
    return query(q, typeFilter)
  })

  ipcMain.handle('search:indexItems', async (_e, type: string, items: any[], targetTab: string) => {
    indexItems(type as any, items, targetTab)
    return { ok: true }
  })
}
```

- [ ] **Step 2: 在 main/index.ts 注册 IPC**

在 `src/main/index.ts` 中：
- 在 import 区添加 `import { registerStatsIpc } from './ipc/stats'`
- 在 `app.whenReady()` 内，其他 register 调用的下方，添加 `registerStatsIpc()`
- 在 `showMainWindow` 函数调用后添加 `resumeTracking()`，在窗口 hide 前添加 `pauseTracking()`
- 在 `createWindow` 函数中 `win.on('close', ...)` 内 `win.hide()` 前添加 `pauseTracking()`

修改 `showMainWindow`：
```ts
function showMainWindow(): void {
  if (!mainWindow || mainWindow.isDestroyed()) {
    mainWindow = createWindow()
  } else {
    ensureRendererReady(mainWindow)
  }
  mainWindow.show()
  if (mainWindow.isMinimized()) mainWindow.restore()
  mainWindow.focus()
  notifyRendererResume(mainWindow)
  resumeTracking()
  hiddenAt = null
}
```

修改 `createWindow` 中 close 事件：
```ts
win.on('close', (event) => {
  if (isQuitting) return
  event.preventDefault()
  pauseTracking()
  hiddenAt = Date.now()
  win.hide()
})
```

在 `app.whenReady()` 末尾添加：
```ts
startTracking()
```

- [ ] **Step 3: Commit**

```bash
git add src/main/ipc/stats.ts src/main/index.ts
git commit -m "feat: add stats and search IPC handlers with tracking hooks"
```

### Task 5: 更新 preload 暴露 stats 和 search API

**Files:**
- Modify: `src/preload/index.ts`
- Modify: `src/preload/api.d.ts`
- Modify: `src/renderer/src/env.d.ts`

- [ ] **Step 1: 在 preload/index.ts 的 api 对象中添加**

在 settings 块之后添加：

```ts
stats: {
  computeDashboard: (payload: any) => ipcRenderer.invoke('stats:computeDashboard', payload),
},
search: {
  query: (q: string, typeFilter?: string) => ipcRenderer.invoke('search:query', q, typeFilter),
  indexItems: (type: string, items: any[], targetTab: string) =>
    ipcRenderer.invoke('search:indexItems', type, items, targetTab),
},
```

- [ ] **Step 2: 更新 api.d.ts**

在 LearnApi 接口中添加：

```ts
stats: {
  computeDashboard: (payload: {
    courses: { id: string; name: string; teacher: string }[]
    homeworksByCourse: Record<string, any[]>
    noticesByCourse: Record<string, any[]>
    discussionsByCourse: Record<string, any[]>
  }) => Promise<any>
}
search: {
  query: (q: string, typeFilter?: string) => Promise<any[]>
  indexItems: (type: string, items: any[], targetTab: string) => Promise<{ ok: boolean }>
}
```

- [ ] **Step 3: Commit**

```bash
git add src/preload/index.ts src/preload/api.d.ts
git commit -m "feat: expose stats and search APIs in preload"
```

### Task 6: Dashboard 接入真实数据

**Files:**
- Modify: `src/renderer/src/pages/Dashboard.tsx`

- [ ] **Step 1: 重写 Dashboard 数据获取逻辑**

在 Dashboard 组件顶部添加数据加载 hook：

```tsx
import { useEffect, useState } from 'react'
import type { StatsSnapshot } from '../../../main/types' // reuse types via IPC result

// Inside component, add:
const [stats, setStats] = useState<StatsSnapshot | null>(null)

useEffect(() => {
  async function load() {
    const courses = useAuthStore.getState().courses
    if (!courses.length) return

    // Gather homework/notices/discussions per course
    const homeworksByCourse: Record<string, any[]> = {}
    const noticesByCourse: Record<string, any[]> = {}
    const discussionsByCourse: Record<string, any[]> = {}

    for (const c of courses) {
      try {
        homeworksByCourse[c.id] = await window.learn.hw.list(c.id)
      } catch { homeworksByCourse[c.id] = [] }
      try {
        noticesByCourse[c.id] = await window.learn.notice.list(c.id)
      } catch { noticesByCourse[c.id] = [] }
      try {
        discussionsByCourse[c.id] = await window.learn.disc.list(c.id)
      } catch { discussionsByCourse[c.id] = [] }
    }

    const result = await window.learn.stats.computeDashboard({
      courses,
      homeworksByCourse,
      noticesByCourse,
      discussionsByCourse,
    })
    setStats(result)
  }
  load()
}, [])
```

- [ ] **Step 2: 替换硬编码数据**

将以下硬编码替换为 stats 数据：
- `focusItems` → `stats.todayFocus`（只展示前 6 条，P0 优先）
- `progressRows` → `stats.courseProgress`
- `updates` → `stats.recentUpdates`
- 进度环 `percent={62}` → `stats.todayMinutes` 等
- `8 / 13` → `stats.completedCourses / stats.totalCourses`
- `18.6 h` → `Math.round(stats.weeklyMinutes / 6) / 10 + ' h'`
- `14 天` → `${stats.streakDays} 天`
- `查看全部任务（6）` → `查看全部任务（${stats.todayFocus.length}）`

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/pages/Dashboard.tsx
git commit -m "feat: wire Dashboard to real stats data"
```

### Task 7: Settings 页添加学期切换

**Files:**
- Modify: `src/renderer/src/pages/Settings.tsx`

- [ ] **Step 1: 在设置页添加学期选择器**

在 Settings 组件中添加学期选择 UI（Select 下拉框，复用 course:semesters IPC）。选择后调用 course:list 刷新课程列表，并将学期 ID 持久化到 settings store。

```tsx
// Add to settings state/effects:
const [semesters, setSemesters] = useState<{ id: string; name: string }[]>([])
const [selectedSemesterId, setSelectedSemesterId] = useState('')

useEffect(() => {
  window.learn.course.listSemesters().then((r) => {
    setSemesters(r.semesters)
    setSelectedSemesterId(r.current.id)
  })
}, [])

// In JSX, add a Select:
<Select
  value={selectedSemesterId}
  onChange={async (id) => {
    setSelectedSemesterId(id)
    await window.learn.settings.set({ lastSemesterId: id })
    // Trigger course reload via store
    window.location.reload() // simplest approach; or dispatch store action
  }}
  options={semesters.map((s) => ({ value: s.id, label: s.name }))}
/>
```

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Settings.tsx
git commit -m "feat: add semester switcher in Settings page"
```

### Task 8: 全局搜索接入 AppShell

**Files:**
- Modify: `src/renderer/src/components/AppShell.tsx`

- [ ] **Step 1: 接入真实搜索**

将 AppShell 中全局搜索输入框绑定真实搜索逻辑：

```tsx
const [searchResults, setSearchResults] = useState<any[]>([])
const [searching, setSearching] = useState(false)

async function handleSearch(value: string) {
  if (!value.trim()) { setSearchResults([]); return }
  setSearching(true)
  const results = await window.learn.search.query(value)
  setSearchResults(results)
  setSearching(false)
}
```

搜索结果以下拉面板展示，点击结果调用 `navigate()` 跳转到对应页面（用 `targetTab` 和 `courseId` 拼接路径）。

- [ ] **Step 2: 课程数据加载后构建搜索索引**

在 Dashboard 数据加载完成后，调用 `search:indexItems` 将各个模块数据注入搜索索引。

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/components/AppShell.tsx
git commit -m "feat: wire global search to real search index"
```

---

## P2: 课件与下载工作流

### Task 9: 课件筛选排序（纯前端）

**Files:**
- Modify: `src/renderer/src/pages/Files.tsx`

- [ ] **Step 1: 添加章节筛选和排序逻辑**

```tsx
const [chapterFilter, setChapterFilter] = useState<string | null>(null)
const [sortOrder, setSortOrder] = useState<'time-desc' | 'time-asc' | 'name-asc' | 'name-desc'>('time-desc')
const [searchText, setSearchText] = useState('')

// Extract unique chapters from file list
const chapters = [...new Set(files.map((f: any) => f.chapter || '未分类'))]

// Filter + sort chain
const filteredFiles = files
  .filter((f: any) => {
    if (chapterFilter && (f.chapter || '未分类') !== chapterFilter) return false
    if (searchText && !f.name.toLowerCase().includes(searchText.toLowerCase())) return false
    return true
  })
  .sort((a: any, b: any) => {
    switch (sortOrder) {
      case 'time-desc': return new Date(b.uploadTime).getTime() - new Date(a.uploadTime).getTime()
      case 'time-asc': return new Date(a.uploadTime).getTime() - new Date(b.uploadTime).getTime()
      case 'name-asc': return a.name.localeCompare(b.name)
      case 'name-desc': return b.name.localeCompare(a.name)
      default: return 0
    }
  })
```

在 JSX 工具栏中添加 Select 控件绑定 `chapterFilter` 和 `sortOrder`。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Files.tsx
git commit -m "feat: add file filter by chapter and sort by time/name"
```

### Task 10: 课件预览 IPC（不落盘）

**Files:**
- Modify: `src/main/ipc/files.ts`

- [ ] **Step 1: 添加 files:preview IPC handler**

在 `files:exists` handler 之后添加：

```ts
import { app as electronApp } from 'electron'
import os from 'os'

ipcMain.handle('files:preview', async (_e, fileId: string, fileName: string, url: string) => {
  // Download to temp directory
  const tempDir = path.join(os.tmpdir(), 'learnpp-preview')
  fs.mkdirSync(tempDir, { recursive: true })
  const tempPath = path.join(tempDir, sanitizeFilename(fileName))

  // If already downloaded to temp, reuse
  if (fs.existsSync(tempPath)) {
    return { tempPath, fileType: path.extname(fileName).toLowerCase() }
  }

  const buffer = await withAuth(async () => {
    return (await import('../services/downloader')).downloadUrlToBuffer(url)
  })

  fs.writeFileSync(tempPath, buffer)
  return { tempPath, fileType: path.extname(fileName).toLowerCase() }
})
```

- [ ] **Step 2: 更新 preload**

在 `src/preload/index.ts` 的 files 块添加：
```ts
preview: (fileId: string, fileName: string, url: string) =>
  ipcRenderer.invoke('files:preview', fileId, fileName, url),
```

在 `src/preload/api.d.ts` 的 files 接口添加：
```ts
preview: (fileId: string, fileName: string, url: string) => Promise<{ tempPath: string; fileType: string }>
```

- [ ] **Step 3: Commit**

```bash
git add src/main/ipc/files.ts src/preload/index.ts src/preload/api.d.ts
git commit -m "feat: add files:preview IPC that downloads to temp dir"
```

### Task 11: Office COM 转 PDF 预览服务

**Files:**
- Create: `src/main/services/office-converter.ts`

- [ ] **Step 1: 创建 office-converter.ts**

```ts
import { execFile } from 'child_process'
import path from 'path'
import fs from 'fs'
import os from 'os'

type OfficeApp = 'Word.Application' | 'PowerPoint.Application' | 'Excel.Application'

function getOfficeApp(ext: string): OfficeApp | null {
  const e = ext.toLowerCase()
  if (e === '.docx' || e === '.doc') return 'Word.Application'
  if (e === '.pptx' || e === '.ppt') return 'PowerPoint.Application'
  if (e === '.xlsx' || e === '.xls') return 'Excel.Application'
  return null
}

export async function convertToPdf(inputPath: string): Promise<string> {
  const ext = path.extname(inputPath)
  const officeApp = getOfficeApp(ext)
  if (!officeApp) throw new Error(`Unsupported file type: ${ext}`)

  const pdfPath = inputPath.replace(ext, '.pdf')

  // Use PowerShell COM automation
  const psScript = `
$app = New-Object -ComObject "${officeApp}"
$app.Visible = $false
$doc = $app.Documents.Open("${inputPath.replace(/\\/g, '\\\\')}")
$doc.ExportAsFixedFormat("${pdfPath.replace(/\\/g, '\\\\')}", 17, $false)
$doc.Close()
$app.Quit()
[System.Runtime.Interopservices.Marshal]::ReleaseComObject($doc) | Out-Null
[System.Runtime.Interopservices.Marshal]::ReleaseComObject($app) | Out-Null
`

  return new Promise((resolve, reject) => {
    const psFile = path.join(os.tmpdir(), `convert-${Date.now()}.ps1`)
    fs.writeFileSync(psFile, psScript)

    execFile('powershell.exe', ['-ExecutionPolicy', 'Bypass', '-File', psFile], { timeout: 60000 }, (err) => {
      try { fs.unlinkSync(psFile) } catch { /* ignore */ }
      if (err) {
        reject(new Error(`COM conversion failed: ${err.message}`))
      } else if (fs.existsSync(pdfPath)) {
        resolve(pdfPath)
      } else {
        reject(new Error('PDF was not generated'))
      }
    })
  })
}

export async function previewFile(tempPath: string, fileType: string): Promise<{ method: 'pdf' | 'image' | 'text'; content: string }> {
  const ext = (fileType || path.extname(tempPath)).toLowerCase()

  // Direct renderable
  if (ext === '.pdf') return { method: 'pdf', content: tempPath }
  if (['.png', '.jpg', '.jpeg', '.gif', '.webp', '.bmp'].includes(ext)) {
    return { method: 'image', content: tempPath }
  }

  // Office -> PDF conversion
  if (['.docx', '.doc', '.pptx', '.ppt', '.xlsx', '.xls'].includes(ext)) {
    try {
      const pdfPath = await convertToPdf(tempPath)
      return { method: 'pdf', content: pdfPath }
    } catch {
      // Fallback: extract text via mammoth/officeparser
      try {
        if (ext === '.docx') {
          const mammoth = require('mammoth')
          const result = await mammoth.extractRawText({ path: tempPath })
          return { method: 'text', content: result.value }
        }
        // For PPT/XLSX, return placeholder
        return { method: 'text', content: 'Office 未安装，无法预览此文件。请安装 Microsoft Office 后重试。' }
      } catch {
        return { method: 'text', content: '预览失败：无法解析此文件。' }
      }
    }
  }

  return { method: 'text', content: '不支持的文件格式' }
}
```

- [ ] **Step 2: 在 files IPC 中添加 files:previewOpen handler**

在 `src/main/ipc/files.ts` 添加：

```ts
ipcMain.handle('files:previewOpen', async (_e, fileId: string, fileName: string, url: string) => {
  const { tempPath, fileType } = await (async () => {
    const tempDir = path.join(os.tmpdir(), 'learnpp-preview')
    fs.mkdirSync(tempDir, { recursive: true })
    const tp = path.join(tempDir, sanitizeFilename(fileName))
    if (fs.existsSync(tp)) return { tempPath: tp, fileType: path.extname(fileName).toLowerCase() }
    const buffer = await withAuth(async () => (await import('../services/downloader')).downloadUrlToBuffer(url))
    fs.writeFileSync(tp, buffer)
    return { tempPath: tp, fileType: path.extname(fileName).toLowerCase() }
  })()

  const { method, content } = await (await import('../services/office-converter')).previewFile(tempPath, fileType)
  return { method, content, fileName }
})
```

- [ ] **Step 3: Commit**

```bash
git add src/main/services/office-converter.ts src/main/ipc/files.ts
git commit -m "feat: add Office COM-to-PDF converter and preview pipeline"
```

### Task 12: 课件预览 UI（BrowserWindow 预览窗口）

**Files:**
- Modify: `src/renderer/src/pages/Files.tsx`

- [ ] **Step 1: 实现预览按钮点击**

```tsx
async function handlePreview(file: any) {
  const result = await window.learn.files.previewOpen(file.fileId || file.id, file.downloadName || file.name, file.downloadUrl)
  if (result.method === 'pdf') {
    // Open a new BrowserWindow to render the PDF
    window.learn.openExternal(`file://${result.content}`)
  } else if (result.method === 'image') {
    window.learn.openExternal(`file://${result.content}`)
  } else {
    message.info(result.content)
  }
}
```

替换 Files.tsx 中预览按钮的 `planned('课件预览')` 为 `handlePreview(file)`。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Files.tsx
git commit -m "feat: wire file preview buttons to real preview pipeline"
```

### Task 13: 批量下载

**Files:**
- Modify: `src/main/ipc/files.ts`
- Modify: `src/renderer/src/pages/Files.tsx`

- [ ] **Step 1: 批量下载 IPC**

在 files IPC 中添加：

```ts
ipcMain.handle('files:batchDownload', async (e, items: { fileId: string; fileName: string; url: string }[]) => {
  const win = BrowserWindow.fromWebContents(e.sender)
  if (!win) throw new Error('No window')

  const CONCURRENCY = 3
  const results: { fileId: string; success: boolean; destPath?: string; error?: string }[] = []

  async function downloadOne(item: { fileId: string; fileName: string; url: string }) {
    try {
      const safeName = sanitizeFilename(item.fileName)
      const downloadId = `${item.fileId}-batch-${Date.now()}`
      const destPath = await withAuth(async () =>
        downloadFile(item.url, downloadDirCache, safeName, win!, downloadId)
      )
      results.push({ fileId: item.fileId, success: true, destPath })
    } catch (err: any) {
      results.push({ fileId: item.fileId, success: false, error: err.message })
    }
  }

  // Process in batches
  for (let i = 0; i < items.length; i += CONCURRENCY) {
    const batch = items.slice(i, i + CONCURRENCY)
    await Promise.all(batch.map(downloadOne))
  }

  return results
})
```

- [ ] **Step 2: 工具栏批量下载按钮**

在 Files.tsx 工具栏的"批量下载"按钮绑定：

```tsx
async function handleBatchDownload() {
  const items = filteredFiles.map((f: any) => ({
    fileId: f.fileId || f.id,
    fileName: f.downloadName || f.name,
    url: f.downloadUrl,
  }))
  message.loading({ content: '批量下载中...', key: 'batch', duration: 0 })
  const results = await window.learn.files.batchDownload(items)
  message.destroy('batch')
  const success = results.filter((r: any) => r.success).length
  message.success(`下载完成：${success}/${results.length}`)
}
```

- [ ] **Step 3: 更新 preload 暴露 batchDownload**

- [ ] **Step 4: Commit**

```bash
git add src/main/ipc/files.ts src/renderer/src/pages/Files.tsx src/preload/index.ts src/preload/api.d.ts
git commit -m "feat: add batch download with 3-concurrency queue"
```

### Task 14: 下载管理页面完善

**Files:**
- Modify: `src/renderer/src/pages/Downloads.tsx`
- Modify: `src/renderer/src/store/downloads.ts`

- [ ] **Step 1: 完善 Downloads 页面**

展示三态列表（进行中/已完成/失败），支持失败重试、打开文件夹、打开文件、清空记录。

使用 `zustand persist` 的 downloads store 已有基础，补充：
- 监听 `files:progress` 事件更新下载状态
- 失败项显示重试按钮
- 已完成项显示"打开文件"和"打开文件夹"按钮

**打开文件 vs 打开文件夹** — 已完成项：
- "打开文件" → `shell.openPath(destPath)` — 新增 IPC `files:openFile`
- "打开文件夹" → `shell.openPath(path.dirname(destPath))` — 复用 `files:openFolder`

```tsx
// 新增 IPC
ipcMain.handle('files:openFile', async (_e, filePath: string) => {
  shell.openPath(filePath)
})
```

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Downloads.tsx src/renderer/src/store/downloads.ts src/main/ipc/files.ts src/preload/index.ts
git commit -m "feat: complete download management page with retry and open file"
```

### Task 15: Bug 修复 — "打开课件"打开文件本身

**Files:**
- Modify: `src/renderer/src/pages/Files.tsx`
- Modify: `src/main/ipc/files.ts`

- [ ] **Step 1: 修复 files:openFolder → 改为打开文件**

已下载文件的"打开课件"按钮应该调用新 IPC `files:openFile` 而不是 `files:openFolder`：

```tsx
// In Files.tsx, for already-downloaded files:
<Button onClick={() => window.learn.files.openFile(destPath)}>打开课件</Button>
```

确保 `files:openFile` IPC 已注册（Task 14 已添加）。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Files.tsx
git commit -m "fix: open file button now opens the file itself, not its folder"
```

---

## P3: 甘蔗 Tutor 全功能

### Task 16: Tutor 风格设置

**Files:**
- Modify: `src/main/types.ts`
- Modify: `src/main/ipc/settings.ts`
- Modify: `src/renderer/src/pages/Tutor.tsx`

- [ ] **Step 1: 在 AppSettings 添加 tutorStyle 字段**

在 `src/main/types.ts` 的 `AppSettings` 接口添加：
```ts
tutorStyle?: 'cute' | 'serious'
```

- [ ] **Step 2: 首次使用弹窗（Tutor.tsx）**

```tsx
const [style, setStyle] = useState<'cute' | 'serious'>('cute')
const [showStylePicker, setShowStylePicker] = useState(false)

useEffect(() => {
  window.learn.settings.getAll().then((s) => {
    if (!s.tutorStyle) {
      setShowStylePicker(true) // First time, show picker
    } else {
      setStyle(s.tutorStyle)
    }
  })
}, [])

function selectStyle(s: 'cute' | 'serious') {
  setStyle(s)
  window.learn.settings.set({ tutorStyle: s })
  setShowStylePicker(false)
}
```

风格选择弹窗：Modal 展示两个选项卡片（可爱风 / 正经风），默认选中可爱风。

- [ ] **Step 3: Settings 页添加风格切换**

在 Settings 页添加 Tutor 风格选择 radio/switch。

- [ ] **Step 4: Commit**

```bash
git add src/main/types.ts src/main/ipc/settings.ts src/renderer/src/pages/Tutor.tsx src/renderer/src/pages/Settings.tsx
git commit -m "feat: add tutor style setting (cute/serious) with first-use picker"
```

### Task 17: System Prompt 构建

**Files:**
- Create: `src/main/services/tutor-prompts.ts`

- [ ] **Step 1: 创建 tutor-prompts.ts**

```ts
export function buildTutorSystemPrompt(
  style: 'cute' | 'serious',
  courseContext?: { name: string; teacher: string }
): string {
  const courseInfo = courseContext
    ? `\n当前课程上下文：${courseContext.name}（教师：${courseContext.teacher}）`
    : ''

  const basePrompt = `你是甘蔗 Tutor，learn++ 桌面客户端内置的 AI 学习助手。你可以通过 function calling 直接操控 learn++ 的各项功能。
${courseInfo}
你有以下工具可用：
- list_courses: 列出所有课程
- list_homeworks: 列出某课程的所有作业
- list_emails: 列出邮件（支持文件夹参数：inbox/sent/drafts/trash）
- search_emails: 搜索邮件
- get_email: 获取邮件详情
- list_files: 列出课程课件
- list_notices: 列出课程公告
- list_discussions: 列出课程讨论
- summarize_content: 总结指定内容
- complete_homework: 帮助完成作业
- search_global: 全局搜索
- get_stats: 获取学习统计

使用工具时：先判断是否需要调用工具 → 如果需要，返回 tool_calls → 等待工具结果 → 基于结果回复用户。
不需要工具时直接回复。

关于作业附件格式判断：默认生成 DOCX。当作业要求明确提到 PDF、涉及复杂数学公式/代码排版、或要求打印提交时，判断应使用 PDF 格式。`

  if (style === 'cute') {
    return `${basePrompt}

你的角色是可爱二次元正太，性格活泼开朗。对话风格：
- 使用"诶嘿～"、"交给我吧！"、"这个我来帮你搞定～"等俏皮开场
- 学术信息保持准确，但用轻松语气包装
- 每条回复末尾标注"仅供学习参考哦～"
- 不要太啰嗦，简短有力`
  }

  return `${basePrompt}

你的角色是专业学术助手。对话风格：
- 简洁直接，不说废话
- 学术信息保持准确严谨
- 每条回复末尾标注"仅供学习参考"
- 用词正式专业，不卖萌`
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main/services/tutor-prompts.ts
git commit -m "feat: add tutor system prompt builder with dual style support"
```

### Task 18: tutor-agent.ts 工具调用编排

**Files:**
- Create: `src/main/services/tutor-agent.ts`

- [ ] **Step 1: 创建 agent 编排器**

```ts
import { ipcMain } from 'electron'
import type { AiMessage } from './ai'
import { buildTutorSystemPrompt } from './tutor-prompts'
import { runAiComplete } from './ai'
import type { AppSettings } from '../types'
import { query } from './search-index'

interface ToolDef {
  name: string
  description: string
  parameters: Record<string, any>
}

const TOOLS: ToolDef[] = [
  { name: 'list_courses', description: '列出所有课程', parameters: {} },
  { name: 'list_homeworks', description: '列出某课程作业', parameters: { courseId: { type: 'string', required: true } } },
  { name: 'list_emails', description: '列出邮件', parameters: { folder: { type: 'string', enum: ['inbox', 'sent', 'drafts', 'trash'] } } },
  { name: 'search_emails', description: '搜索邮件', parameters: { query: { type: 'string', required: true } } },
  { name: 'get_email', description: '获取邮件详情', parameters: { mailId: { type: 'string', required: true } } },
  { name: 'list_files', description: '列出课程课件', parameters: { courseId: { type: 'string', required: true } } },
  { name: 'list_notices', description: '列出课程公告', parameters: { courseId: { type: 'string', required: true } } },
  { name: 'list_discussions', description: '列出课程讨论', parameters: { courseId: { type: 'string', required: true } } },
  { name: 'summarize_content', description: '总结指定内容', parameters: { content: { type: 'string', required: true }, kind: { type: 'string' } } },
  { name: 'complete_homework', description: '帮助完成作业', parameters: { courseId: { type: 'string', required: true }, homeworkId: { type: 'string' } } },
  { name: 'search_global', description: '全局搜索', parameters: { q: { type: 'string', required: true } } },
  { name: 'get_stats', description: '获取学习统计', parameters: {} },
]

function toolsToOpenAI(): any[] {
  return TOOLS.map((t) => ({
    type: 'function',
    function: { name: t.name, description: t.description, parameters: { type: 'object', properties: t.parameters } },
  }))
}
```

由于 plan 篇幅限制，完整 agent 循环逻辑见实现时展开。核心循环：
1. 用户消息 → 构建 messages（system + history + user）
2. 调用 AI（带 tool definitions）→ 流式输出 text + 可能的 tool_calls
3. 如有 tool_calls → 执行对应 IPC → 结果注入 messages → 回到步骤 2
4. 无 tool_calls → 流式输出完成

- [ ] **Step 2: Commit**

```bash
git add src/main/services/tutor-agent.ts
git commit -m "feat: add tutor agent tool-calling orchestration"
```

### Task 19: Tutor 对话流实现

**Files:**
- Modify: `src/renderer/src/pages/Tutor.tsx`
- Modify: `src/main/ipc/ai.ts`
- Create: `src/renderer/src/store/tutor.ts`

- [ ] **Step 1: 创建 tutor Zustand store**

创建 `src/renderer/src/store/tutor.ts`：

```ts
import { create } from 'zustand'

interface TutorMessage { role: 'user' | 'assistant'; content: string }

interface TutorState {
  messages: TutorMessage[]
  streaming: boolean
  style: 'cute' | 'serious'
  addMessage: (msg: TutorMessage) => void
  setStreaming: (v: boolean) => void
  setStyle: (s: 'cute' | 'serious') => void
  clearMessages: () => void
}

export const useTutorStore = create<TutorState>((set) => ({
  messages: [],
  streaming: false,
  style: 'cute',
  addMessage: (msg) => set((s) => ({ messages: [...s.messages, msg] })),
  setStreaming: (v) => set({ streaming: v }),
  setStyle: (style) => set({ style }),
  clearMessages: () => set({ messages: [] }),
}))
```

- [ ] **Step 2: 添加 tutor:chat IPC**

在 `src/main/ipc/ai.ts` 添加：

```ts
import { runAgentChat } from '../services/tutor-agent'

ipcMain.handle('tutor:chat', async (e, payload: {
  messages: { role: string; content: string }[]
  courseId?: string
  style: 'cute' | 'serious'
  sessionId: string
}) => {
  const win = BrowserWindow.fromWebContents(e.sender)
  // ... agent loop calling runAiComplete with tools, streaming chunks back
})
```

- [ ] **Step 3: Tutor.tsx 对话 UI**

```tsx
const { messages, addMessage, streaming, setStreaming } = useTutorStore()
const [input, setInput] = useState('')

async function sendMessage() {
  const text = input.trim()
  if (!text || streaming) return
  setInput('')
  addMessage({ role: 'user', content: text })
  setStreaming(true)

  const sessionId = `tutor-${Date.now()}`
  let assistantContent = ''
  addMessage({ role: 'assistant', content: '' })

  const unsubChunk = window.learn.hwai.onChunk(({ sessionId: sid, delta }) => {
    if (sid !== sessionId) return
    assistantContent += delta
    useTutorStore.setState((s) => {
      const msgs = [...s.messages]
      msgs[msgs.length - 1] = { role: 'assistant', content: assistantContent }
      return { messages: msgs }
    })
  })

  const unsubEnd = window.learn.hwai.onEnd(({ sessionId: sid }) => {
    if (sid !== sessionId) return
    unsubChunk(); unsubEnd()
    setStreaming(false)
  })

  await window.learn.hwai.generate({
    sessionId, messages: useTutorStore.getState().messages.slice(0, -1),
    style: useTutorStore.getState().style,
  })
}
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/src/store/tutor.ts src/renderer/src/pages/Tutor.tsx src/main/ipc/ai.ts
git commit -m "feat: implement tutor chat flow with streaming rendering"
```

### Task 20: Tutor 上下文管理 + 快捷功能

**Files:**
- Modify: `src/renderer/src/pages/Tutor.tsx`

- [ ] **Step 1: 上下文注入 + "新对话"按钮**

```tsx
const selectedCourseId = useAuthStore((s) => s.selectedCourseId)

// Pass course context to agent (handled in main process system prompt)
// "新对话" button:
<Button onClick={() => useTutorStore.getState().clearMessages()}>新对话</Button>
```

Agent 侧（tutor-agent.ts）读取当前 courseId 并注入 system prompt。

- [ ] **Step 2: 快捷功能按钮实现**

```tsx
const QUICK_ACTIONS: Record<string, string> = {
  '知识总结': '请帮我总结当前课程的核心知识点',
  '全局辅助': '我现在需要什么帮助？请根据我的课程情况给建议',
  '出题练习': '请根据当前课程内容出几道练习题',
  '学习计划': '请帮我制定一个学习计划',
  '解释概念': '请解释以下概念：',
}

function handleQuickAction(action: string) {
  const promptText = QUICK_ACTIONS[action]
  setInput(promptText)
  // auto-submit after brief delay
  setTimeout(() => sendMessage(), 100)
}
```

在 Tutor 页右侧功能列表中绑定点击事件。

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/pages/Tutor.tsx
git commit -m "feat: add tutor context injection, new chat, and quick action buttons"
```

### Task 21: 总结功能接入（弹窗展示）

**Files:**
- Modify: `src/renderer/src/pages/Files.tsx`
- Modify: `src/renderer/src/pages/Notifications.tsx`
- Modify: `src/renderer/src/pages/Discussion.tsx`

- [ ] **Step 1: 创建 SummaryModal 组件内联实现**

在三个页面中分别在 Tutor 总结按钮的 onClick 中打开 Ant Design Modal，调用 `hwai:tutor-summary`：

```tsx
const [summaryOpen, setSummaryOpen] = useState(false)
const [summaryText, setSummaryText] = useState('')
const [summaryLoading, setSummaryLoading] = useState(false)

async function handleTutorSummary() {
  setSummaryOpen(true)
  setSummaryLoading(true)
  setSummaryText('')
  try {
    const result = await window.learn.hwai.tutorSummary(courseId, 'files')
    setSummaryText(result.content || '总结生成失败')
  } catch (err: any) {
    setSummaryText('总结生成失败：' + err.message)
  }
  setSummaryLoading(false)
}

// Modal:
<Modal open={summaryOpen} onCancel={() => setSummaryOpen(false)} title="甘蔗 Tutor 总结" footer={null}>
  {summaryLoading ? <Spin /> : <div>{summaryText}</div>}
</Modal>
```

Files.tsx 传 `kind: 'files'`，Notifications.tsx 传 `kind: 'notifications'`，Discussion.tsx 传 `kind: 'discussion'`。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Files.tsx src/renderer/src/pages/Notifications.tsx src/renderer/src/pages/Discussion.tsx
git commit -m "feat: wire tutor summary buttons to modal display on files/notices/discussion pages"
```

### Task 22: 一键作业上下文收集

**Files:**
- Modify: `src/main/services/homework-ai.ts`

- [ ] **Step 1: 上下文收集函数**

```ts
async function collectHomeworkContext(
  hw: { id: string; studentHomeworkId: string; title: string; description?: string; attachments?: { name: string; url: string }[]; courseId: string },
): Promise<{ description: string; attachmentTexts: { name: string; text: string }[]; referencedFiles: string[] }> {
  const description = hw.description || ''

  // Parse teacher attachments
  const attachmentTexts: { name: string; text: string }[] = []
  if (hw.attachments) {
    for (const att of hw.attachments) {
      try {
        const buffer = await downloadUrlToBuffer(att.url)
        const ext = att.name.toLowerCase()
        if (ext.endsWith('.docx')) {
          const mammothResult = await extractTextFromDocx(buffer)
          attachmentTexts.push({ name: att.name, text: mammothResult })
        } else if (ext.endsWith('.pdf')) {
          const pdfResult = await extractTextFromPdf(buffer)
          attachmentTexts.push({ name: att.name, text: pdfResult })
        }
      } catch (err) {
        attachmentTexts.push({ name: att.name, text: `[无法解析: ${err}]` })
      }
    }
  }

  // Detect courseware references in description
  const referencedFiles: string[] = []
  const refPatterns = [/参考.{0,5}第.{0,3}[章节]/, /见课件/, /课件.{0,5}PPT/, /教材.{0,3}第/]
  for (const pattern of refPatterns) {
    if (pattern.test(description)) {
      referencedFiles.push('课件引用已检测到')
      break
    }
  }

  return { description, attachmentTexts, referencedFiles }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main/services/homework-ai.ts
git commit -m "feat: implement homework context collection (description + attachments + file refs)"
```

### Task 23: 一键作业生成 + 格式判断

**Files:**
- Modify: `src/main/services/homework-ai.ts`
- Modify: `src/renderer/src/pages/HomeworkAutoComplete.tsx`

- [ ] **Step 1: 格式判断 skill（prompt 内建）**

在 tutor-prompts.ts 的 complete_homework 工具描述中嵌入格式判断逻辑：

```ts
function buildFormatDecisionPrompt(description: string, attachmentTexts: { name: string; text: string }[]): string {
  const allText = description + '\n' + attachmentTexts.map((a) => a.text).join('\n')
  return `Based on the homework requirements below, determine the output format:
- Default: DOCX
- Use PDF if: requirement explicitly says PDF, involves complex math/code, or requires printing
Requirements: ${allText.slice(0, 2000)}`
}
```

- [ ] **Step 2: HomeworkAutoComplete 完整流程**

```tsx
async function startGenerate(hw: any) {
  // 1. Show academic integrity modal
  const acked = await window.learn.hwai.hasAcknowledgedRisk()
  if (!acked) {
    // Show RiskDisclaimerModal (yellow, not red)
    // Wait for user acknowledgment
    await window.learn.hwai.acknowledgeRisk()
  }

  // 2. Context collection progress
  message.loading({ content: '正在收集作业上下文...', key: 'hw-gen' })

  // 3. Generate
  const sessionId = `hw-${hw.studentHomeworkId}-${Date.now()}`
  const unsubChunk = window.learn.hwai.onChunk((data) => {
    // Stream answer text to UI
  })
  const unsubEnd = window.learn.hwai.onEnd((data) => {
    unsubChunk(); unsubEnd()
    message.success({ content: '生成完成，仅供参考', key: 'hw-gen' })
  })

  await window.learn.hwai.generate({
    analyzed: hw, sessionId,
  })
}
```

- [ ] **Step 3: Commit**

```bash
git add src/main/services/homework-ai.ts src/renderer/src/pages/HomeworkAutoComplete.tsx
git commit -m "feat: complete homework auto-generate with format decision and integrity check"
```

### Task 24: P3 IPC 注册与 preload 更新

**Files:**
- Modify: `src/main/ipc/ai.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/preload/api.d.ts`

- [ ] **Step 1: 注册 tutor:chat IPC**

在 ai.ts 中注册完整的 `tutor:chat` handler（含 abort 支持）。
更新 preload 暴露 `tutor:chat`、`tutor:abort`。

- [ ] **Step 2: Commit**

```bash
git add src/main/ipc/ai.ts src/preload/index.ts src/preload/api.d.ts
git commit -m "feat: register tutor:chat IPC with abort support"

---

## P4: 邮箱真实接入

### Task 26: mail-service.ts 邮件服务

**Files:**
- Create: `src/main/services/mail-service.ts`

- [ ] **Step 1: 创建 mail-service.ts**

```ts
import { BrowserWindow, session } from 'electron'
import { getCookieString } from './learn'

const MAIL_BASE = 'https://mails.tsinghua.edu.cn'

interface MailItem {
  id: string
  subject: string
  from: string
  to: string
  date: string
  preview: string
  starred: boolean
  read: boolean
}

interface MailDetail extends MailItem {
  body: string
  attachments: { name: string; url: string }[]
}

let mailCookies: string | null = null
let listCache: { folder: string; mails: MailItem[]; cachedAt: number } | null = null
const detailCache: Map<string, { mail: MailDetail; cachedAt: number }> = new Map()

const LIST_CACHE_MS = 5 * 60 * 1000
const DETAIL_CACHE_MS = 30 * 60 * 1000

export async function loginMail(): Promise<boolean> {
  return new Promise((resolve) => {
    const win = new BrowserWindow({
      width: 800, height: 600, title: '登录清华邮箱',
      webPreferences: { nodeIntegration: false, contextIsolation: true },
    })
    win.loadURL(MAIL_BASE)
    win.on('closed', async () => {
      try {
        const cookies = await session.defaultSession.cookies.get({ url: MAIL_BASE })
        if (cookies.length > 0) {
          mailCookies = cookies.map((c) => `${c.name}=${c.value}`).join('; ')
          resolve(true)
        } else {
          resolve(false)
        }
      } catch { resolve(false) }
    })
    // Auto-detect login: poll for cookie presence
    const checkInterval = setInterval(async () => {
      const cookies = await session.defaultSession.cookies.get({ url: MAIL_BASE })
      if (cookies.some((c) => c.name.includes('sid') || c.name.includes('token') || c.name.includes('TSC'))) {
        clearInterval(checkInterval)
        mailCookies = cookies.map((c) => `${c.name}=${c.value}`).join('; ')
        win.close()
        resolve(true)
      }
    }, 3000)
    // Timeout after 5 minutes
    setTimeout(() => { clearInterval(checkInterval); win.close(); resolve(false) }, 5 * 60 * 1000)
  })
}

// Note: The actual HTTP scraping of mails.tsinghua.edu.cn will depend on the
// specific webmail interface. The initial implementation uses generic patterns.
// Adjust selectors/endpoints based on actual HTML structure observed.

export async function getMailList(folder: string): Promise<{ mails: MailItem[]; total: number }> {
  if (!mailCookies) throw new Error('Mail not logged in')
  if (listCache && listCache.folder === folder && Date.now() - listCache.cachedAt < LIST_CACHE_MS) {
    return { mails: listCache.mails, total: listCache.mails.length }
  }
  const url = `${MAIL_BASE}/?folder=${encodeURIComponent(folder)}`
  const html = await fetchWithCookies(url)
  const mails = parseMailListHtml(html)
  listCache = { folder, mails, cachedAt: Date.now() }
  return { mails, total: mails.length }
}

export async function getMailDetail(mailId: string): Promise<MailDetail | null> {
  if (!mailCookies) return null
  const cached = detailCache.get(mailId)
  if (cached && Date.now() - cached.cachedAt < DETAIL_CACHE_MS) return cached.mail
  const url = `${MAIL_BASE}/?read=${encodeURIComponent(mailId)}`
  const html = await fetchWithCookies(url)
  const mail = parseMailDetailHtml(html, mailId)
  if (mail) detailCache.set(mailId, { mail, cachedAt: Date.now() })
  return mail
}

export async function setMailStarred(mailId: string, starred: boolean): Promise<{ ok: boolean }> {
  if (!mailCookies) return { ok: false }
  await postWithCookies(`${MAIL_BASE}/?star=${mailId}`, { starred })
  listCache = null
  return { ok: true }
}

export async function deleteMail(mailId: string): Promise<{ ok: boolean }> {
  if (!mailCookies) return { ok: false }
  await postWithCookies(`${MAIL_BASE}/?delete=${mailId}`, {})
  listCache = null
  detailCache.delete(mailId)
  return { ok: true }
}

async function fetchWithCookies(url: string): Promise<string> {
  const resp = await fetch(url, { headers: { Cookie: mailCookies! } })
  return resp.text()
}

async function postWithCookies(url: string, body: any): Promise<string> {
  const resp = await fetch(url, {
    method: 'POST',
    headers: { Cookie: mailCookies!, 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams(body).toString(),
  })
  return resp.text()
}

function parseMailListHtml(html: string): MailItem[] { return [] }
function parseMailDetailHtml(html: string, id: string): MailDetail | null { return null }
```

HTML 解析函数的具体实现取决于 mails.tsinghua.edu.cn 的实际 DOM 结构，需在真实登录后通过 DevTools 确认 selector。

- [ ] **Step 2: Commit**

```bash
git add src/main/services/mail-service.ts
git commit -m "feat: add mail service with BrowserWindow login and cookie capture"
```

### Task 27: mail IPC 注册

**Files:**
- Create: `src/main/ipc/mail.ts`

- [ ] **Step 1: 创建 mail IPC**

```ts
import { ipcMain } from 'electron'
import { loginMail } from '../services/mail-service'

export function registerMailIpc(): void {
  ipcMain.handle('mail:login', async () => {
    return { ok: await loginMail() }
  })

  ipcMain.handle('mail:list', async (_e, folder: string) => {
    const { getMailList } = await import('../services/mail-service')
    return getMailList(folder)
  })

  ipcMain.handle('mail:get', async (_e, mailId: string) => {
    const { getMailDetail } = await import('../services/mail-service')
    return getMailDetail(mailId)
  })

  ipcMain.handle('mail:star', async (_e, mailId: string, starred: boolean) => {
    const { setMailStarred } = await import('../services/mail-service')
    return setMailStarred(mailId, starred)
  })

  ipcMain.handle('mail:delete', async (_e, mailId: string) => {
    const { deleteMail } = await import('../services/mail-service')
    return deleteMail(mailId)
  })
}
```

在 `src/main/index.ts` 中注册：`import { registerMailIpc } from './ipc/mail'` 并在 `app.whenReady()` 中调用。

- [ ] **Step 2: 更新 preload 暴露 mail API**

- [ ] **Step 3: Commit**

```bash
git add src/main/ipc/mail.ts src/main/index.ts src/preload/index.ts src/preload/api.d.ts
git commit -m "feat: add mail IPC handlers and preload API"
```

### Task 28: 邮箱左侧导航子菜单动画

**Files:**
- Modify: `src/renderer/src/components/AppShell.tsx`
- Modify: `src/renderer/src/styles/tsinghua.css`

- [ ] **Step 1: 实现子菜单展开动画**

在 AppShell 的"邮箱"导航项添加状态：

```tsx
const [mailSubOpen, setMailSubOpen] = useState(false)
const [mailFolder, setMailFolder] = useState<string>('inbox')
```

点击"邮箱"时：
- 如果子菜单未展开：展开子菜单（非线性动画），默认进入收件箱
- 如果子菜单已展开：收起

子菜单项：收件箱 / 草稿箱 / 已发送 / 已删除

CSS 动画使用 CSS transition + max-height：

```css
.mail-submenu {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.35s cubic-bezier(0.16, 1, 0.3, 1);
}
.mail-submenu.open {
  max-height: 200px;
}
```

点击子项后设置 `mailFolder`，收起子菜单，导航到邮箱页并传递 folder 参数。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/components/AppShell.tsx src/renderer/src/styles/tsinghua.css
git commit -m "feat: add animated mail folder submenu in sidebar navigation"
```

### Task 29: Mailbox.tsx 接入真实数据

**Files:**
- Modify: `src/renderer/src/pages/Mailbox.tsx`

- [ ] **Step 1: 替换模拟数据**

将硬编码的邮件列表替换为 `window.learn.mail.list(folder, page)` 调用。

星标操作：`window.learn.mail.star(mailId, !starred)`
删除操作：`window.learn.mail.delete(mailId)`

搜索：renderer 侧从缓存列表过滤，或调用 `mail:search`。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Mailbox.tsx
git commit -m "feat: wire Mailbox UI to real mail IPC"
```

---

## P5: UI 统一、动画与 Bug 修复

### Task 30: 全局非线性动画 CSS 变量注入

**Files:**
- Modify: `src/renderer/src/styles/tsinghua.css`

- [ ] **Step 1: 在 tsinghua.css 顶部注入动画变量**

```css
:root {
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

- [ ] **Step 2: 全局应用动画**

应用 `transition` 和 `animation` 使用 `var(--ease-out)`：
- 窗口控制按钮 hover/active
- 侧栏展开/折叠
- 弹窗/抽屉出入（Ant Design Modal/Drawer wrap）
- 卡片 hover 阴影变化
- 登录页元素入场
- 按钮 hover/active 状态

```css
/* Example: all interactive elements */
button, a, .ant-btn, .lp2-card, .ant-modal, .ant-drawer {
  transition-timing-function: var(--ease-out);
  transition-duration: 0.3s;
}

/* Window controls */
.window-control-btn {
  transition: all 0.25s var(--ease-out);
}

/* Modal / Drawer */
.ant-modal-enter, .ant-drawer-enter {
  animation-timing-function: var(--ease-out) !important;
}
```

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/styles/tsinghua.css
git commit -m "feat: add global non-linear animation CSS variables and apply to all components"
```

### Task 31: 窗口控制按钮修复

**Files:**
- Modify: `src/renderer/src/components/WindowControls.tsx`
- Modify: `src/main/ipc/app.ts`

- [ ] **Step 1: 排查并修复**

根因分析：窗口按钮失效通常由以下原因：
1. IPC 调用竞态：快速连点造成多个 `invoke` 同时进行
2. drag 区域冲突：`-webkit-app-region: drag` 区域覆盖了按钮区域
3. 事件冒泡：按钮 click 被父级 drag 事件拦截

修复方案：

```tsx
// WindowControls.tsx - 确保按钮不可拖拽 + 防抖
function WindowControls() {
  const handleMinimize = useCallback(async () => {
    try { await window.learn.app.minimizeWindow() } catch {}
  }, [])

  const handleToggleMaximize = useCallback(async () => {
    try { await window.learn.app.toggleMaximizeWindow() } catch {}
  }, [])

  const handleClose = useCallback(async () => {
    try { await window.learn.app.closeWindow() } catch {}
  }, [])

  return (
    <div className="window-controls" style={{ WebkitAppRegion: 'no-drag' as any }}>
      <button
        className="window-control-btn minimize"
        onClick={handleMinimize}
        style={{ WebkitAppRegion: 'no-drag' as any }}
      >
        <MinimizeIcon />
      </button>
      <button
        className="window-control-btn maximize"
        onClick={handleToggleMaximize}
        style={{ WebkitAppRegion: 'no-drag' as any }}
      >
        <MaximizeIcon />
      </button>
      <button
        className="window-control-btn close"
        onClick={handleClose}
        style={{ WebkitAppRegion: 'no-drag' as any }}
      >
        <CloseIcon />
      </button>
    </div>
  )
}
```

CSS 确保：
```css
.window-controls,
.window-control-btn {
  -webkit-app-region: no-drag !important;
}
```

在 app.ts 的 IPC handler 中添加错误保护：

```ts
ipcMain.handle('window:minimize', async () => {
  const win = BrowserWindow.getFocusedWindow()
  if (win && !win.isDestroyed()) win.minimize()
})

ipcMain.handle('window:toggle-maximize', async () => {
  const win = BrowserWindow.getFocusedWindow()
  if (!win || win.isDestroyed()) return
  if (win.isMaximized()) win.unmaximize()
  else win.maximize()
})
```

- [ ] **Step 2: 窗口回归测试**

手动测试：
- 最小化 → 恢复 → 再次最小化
- 最大化 → 还原 → 再次最大化
- 关闭主窗口 → 检查托盘图标 → 托盘打开恢复
- 拖拽标题栏空白区域 → 窗口移动
- 点击按钮/输入框/图片 → 不触发拖拽

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/components/WindowControls.tsx src/main/ipc/app.ts src/renderer/src/styles/tsinghua.css
git commit -m "fix: window control buttons - no-drag, debounce, null guards in IPC"
```

### Task 32: 页面 2.0 风格统一

**Files:**
- Modify: `src/renderer/src/pages/Notifications.tsx`
- Modify: `src/renderer/src/pages/Homework.tsx`
- Modify: `src/renderer/src/pages/HomeworkDetail.tsx`
- Modify: `src/renderer/src/pages/Discussion.tsx`
- Modify: `src/renderer/src/pages/Questionnaire.tsx`

- [ ] **Step 1: 统一各页面工具栏和卡片风格**

将以下页面的工具栏与 Files.tsx 保持一致（搜索框 + 筛选 + 操作按钮行）：
- Notifications.tsx：添加搜索框 + Tutor 总结按钮
- Homework.tsx：添加搜索框 + 筛选（截止状态）+ Tutor 辅助按钮
- HomeworkDetail.tsx：统一卡片圆角/阴影/边框
- Discussion.tsx：添加搜索框 + Tutor 总结按钮
- Questionnaire.tsx：2.0 卡片风格、搜索框、状态标签

统一使用 CSS class：`lp2-toolbar`、`lp2-card`、`lp2-card-title`

- [ ] **Step 2: Commit**

```bash
git add src/renderer/src/pages/Notifications.tsx src/renderer/src/pages/Homework.tsx \
  src/renderer/src/pages/HomeworkDetail.tsx src/renderer/src/pages/Discussion.tsx \
  src/renderer/src/pages/Questionnaire.tsx
git commit -m "style: unify toolbar and card styles across all course tab pages"
```

### Task 33: CSS 模块化拆分

**Files:**
- Create: `src/renderer/src/styles/window-controls.css`
- Create: `src/renderer/src/styles/layout.css`
- Modify: `src/renderer/src/styles/tsinghua.css`
- Modify: `src/renderer/src/styles/global.css`

- [ ] **Step 1: 提取窗口控制样式到 window-controls.css**

从 tsinghua.css 提取 `.window-controls`、`.window-control-btn` 及其变体样式。

- [ ] **Step 2: 提取布局/导航样式到 layout.css**

从 tsinghua.css 提取 `.lp2-sidebar`、`.lp2-nav`、`.lp2-main`、`.lp2-header` 等布局相关样式。

- [ ] **Step 3: 在 global.css 中 @import 新文件**

```css
@import './window-controls.css';
@import './layout.css';
```

- [ ] **Step 4: 清理 tsinghua.css 重复规则**

- 删除已移至 window-controls.css 和 layout.css 的样式
- 合并重复的 `.lp2-*` 覆盖规则
- 减少 `!important` 使用
- 保留关键约束注释

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/styles/
git commit -m "refactor: split CSS into modular files and clean up duplicate rules"
```

### Task 34: 最终集成验证

- [ ] **Step 1: 运行 typecheck**

```bash
npm run typecheck
```

修复所有类型错误。

- [ ] **Step 2: 运行 build 验证**

```bash
npm run build
```

确保无构建错误。

- [ ] **Step 3: 运行 package:dir 验证打包**

```bash
npm run package:dir
```

确认 `dist-installer/win-unpacked/learn++.exe` 生成。

- [ ] **Step 4: 功能验证清单**

- 登录正常
- Dashboard 展示真实统计数据
- 全局搜索可搜索并跳转
- 课件筛选/排序/预览/批量下载
- 甘蔗 Tutor 对话流 + 工具调用 + 一键作业
- 邮箱登录/列表/详情/星标/删除
- 窗口控制按钮全部正常
- 学期切换
- 非线性动画在所有交互中出现

- [ ] **Step 5: 最终 Commit**

```bash
git add -A
git commit -m "feat: complete 2.0 full implementation with stats, preview, tutor agent, email, and animations"
```
