# 后台静默与托盘退出记录

## 背景

桌面客户端需要更符合常驻应用的使用习惯：点击窗口右上角关闭按钮时，不应直接退出程序；开机自启动时也不应主动弹窗打扰用户。

## 修复思路

- 主进程新增系统托盘图标。
- 窗口关闭事件改为隐藏窗口，程序继续在后台保活。
- 托盘菜单提供“打开 learn++”和“彻底退出”。
- 开机自启动写入 `--hidden` 参数，启动后默认隐藏主窗口。

## 结果

- 点击右上角叉号后，learn++ 会进入后台静默运行。
- Windows 右下角托盘图标可右键彻底退出。
- 开机自启动时默认进入后台，不主动弹出主窗口。
